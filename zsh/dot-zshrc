### Zinit

if [[ ! -f $HOME/.zinit/bin/zinit.zsh ]]; then
    print -P "%F{33}▓▒░ %F{220}Installing %F{33}DHARMA%F{220} Initiative Plugin Manager (%F{33}zdharma/zinit%F{220})…%f"
    command mkdir -p "$HOME/.zinit" && command chmod g-rwX "$HOME/.zinit"
    command git clone https://github.com/zdharma/zinit "$HOME/.zinit/bin" && \
        print -P "%F{33}▓▒░ %F{34}Installation successful.%f%b" || \
        print -P "%F{160}▓▒░ The clone has failed.%f%b"
fi

source "$HOME/.zinit/bin/zinit.zsh"
# plugins
zinit light denysdovhan/spaceship-prompt
zinit ice wait lucid atload"zicompinit"
zinit light zdharma/fast-syntax-highlighting
# directory snippets
zinit ice svn wait lucid; zinit snippet OMZ::plugins/pip
zinit ice svn wait lucid; zinit snippet OMZ::plugins/z
# single-file snippets
zinit for \
	OMZ::lib/completion.zsh \
	OMZ::lib/directories.zsh \
	OMZ::lib/history.zsh \
	OMZ::lib/key-bindings.zsh \
	OMZ::lib/theme-and-appearance.zsh \
	OMZ::plugins/git/git.plugin.zsh
zinit ice wait lucid atload"alias cat=ccat; alias less=cless"
zinit snippet OMZ::plugins/colorize/colorize.plugin.zsh
zinit ice wait lucid; zinit snippet OMZ::plugins/colored-man-pages/colored-man-pages.plugin.zsh
zinit ice wait lucid; zinit snippet OMZ::plugins/extract/extract.plugin.zsh
zinit ice wait lucid; zinit snippet OMZ::plugins/pyenv/pyenv.plugin.zsh

### Zinit END
#
### User Configuration

## Colorize
ZSH_COLORIZE_STYLE=vim
ZSH_COLORIZE_TOOL=chroma
## Conda
# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/root/.miniconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/root/.miniconda3/etc/profile.d/conda.sh" ]; then
        . "/root/.miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/root/.miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<
## N
export N_PREFIX="$HOME/.n"
export PATH="$N_PREFIX/bin:$PATH"
## Poetry
export PATH="$HOME/.poetry/bin:$PATH"
## Pyenv
export PYENV_VIRTUALENV_DISABLE_PROMPT=1
## Shorthands
alias cls="clear"
alias prch="proxychains"
if command -v stow &>/dev/null && `which stow` -V | grep 2.3 &>/dev/null; then
	alias stow="stow --dotfiles"
fi
## Spaceship-Prompt
SPACESHIP_PROMPT_ORDER=(
	user
	host
	dir
	git
	node
	golang
	pyenv
	line_sep
	jobs
	exit_code
	char
)
SPACESHIP_DIR_LOCK_SYMBOL=" 🔒"
SPACESHIP_DIR_TRUNC=0
SPACESHIP_DIR_TRUNC_REPO=false
SPACESHIP_EXIT_CODE_SHOW=true
SPACESHIP_HOST_PREFIX="@ "
SPACESHIP_HOST_SHOW="always"
SPACESHIP_PROMPT_ADD_NEWLINE=false
SPACESHIP_USER_SHOW="always"
SPACESHIP_USER_COLOR_ROOT="yellow"
## WSL2 
if [[ $(grep microsoft /proc/version) ]]; then
	export HOST_IP=$(cat /etc/resolv.conf | tail -n 1 | sed -e "s/nameserver //g")
	alias e="explorer.exe"
	alias ed="explorer.exe ."
	alias prci="sudo sed -i \"$ s/.*/socks5 $HOST_IP 7890/g\" /etc/proxychains4.conf"
fi

### User Configuration END



